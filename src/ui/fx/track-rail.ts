export const TRACK_MARKER_SIZE = 7

export const TRACK_TICK_PERIOD = 15

export const TRACK_TICK_STYLE: React.CSSProperties = {
  backgroundImage: `repeating-linear-gradient(180deg, var(--color-edge) 0 1px, transparent 1px ${TRACK_TICK_PERIOD}px)`,
}
